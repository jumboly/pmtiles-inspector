# main への push で GitHub Pages にデプロイする。
# 公式実装との比較テストを通ったものだけを公開するため、build の前に test を走らせる。
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# 連続 push で古いデプロイが後から上書きしないよう、同時実行は 1 つに絞る
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    # ubuntu-latest は 2026-10〜11 に 26.04 へ切り替わる。移行の途中で環境が変わらないよう明示する
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          # ローカル開発と同じメジャー。テストが Node の DecompressionStream（brotli 対応）に依存するため揃える
          node-version: 25
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build
      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-26.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
